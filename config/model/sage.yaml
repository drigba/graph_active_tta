defaults:
  - base_sage
  - trainer: sgd

hidden_dims: [64]
dropout: 0.8
# dropout: 0.0
inplace: False
cached: True
name: sage

trainer:
  optimizer:
    lr: 1e-2
    weight_decay: 1e-4
  early_stopping:
    patience: 50
    save_model_state: True